version: 2

models:
  - name: dim1_token
    description: Dimension table containing metadata for tokens.
    columns:
      - name: id
        description: The ID of the token.
        config:
          meta: {}
          tags: []
        data_type: STRING

      - name: token_symbol
        description: The symbol for the token
        config:
          meta: {}
          tags: []
        data_type: STRING

      - name: token_address
        description: The address identifier for the token
        config:
          meta: {}
          tags: []
        data_type: STRING

      - name: token_name
        description: The name of the token
        config:
          meta: {}
          tags: []
        data_type: STRING